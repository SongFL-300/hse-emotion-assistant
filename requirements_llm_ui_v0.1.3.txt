# 情绪感知 LLM 对话 + UI 模块（v0.1.3）依赖
# 安装方式（在你的 conda env 里）：
#   pip install -r requirements_llm_ui_v0.1.3.txt
#
# v0.1.3：新增 Emotion Queue（情绪变化队列注入到提示词）；新增“纯语音对话模式”（ASR 自动发送 + TTS 低延时流式播放）
dashscope
websockets>=11
sounddevice
pillow>=10.0.0

# 情感分析项目环境依赖
# 请使用以下命令安装：
# pip install -r requirements_stable.txt

# 核心深度学习框架
tensorflow==2.15.0      # 必须匹配
keras==2.15.0           # 通常随TF安装，但显式指定更安全

# 计算机视觉与图像处理
mediapipe==0.10.9       # 关键版本，兼容TF2.15和Protobuf
opencv-python==4.9.0.80 # 必须 < 4.10 以兼容 NumPy 1.x
opencv-contrib-python==4.9.0.80
pillow>=10.0.0

# 科学计算基础库
numpy==1.26.4           # 必须 < 2.0，否则TF会崩溃
pandas>=1.5.0
scikit-learn>=1.0.0

# 协议缓冲区 (版本冲突的高发区)
protobuf==3.20.3        # 必须准确匹配，TF和MediaPipe的公约数

# 其他工具
requests>=2.31.0
tqdm>=4.65.0
imutils>=0.5.3
h5py==3.10.0            # 用于模型加载
